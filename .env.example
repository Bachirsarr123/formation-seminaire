DATABASE_URL="postgresql://user:password@localhost:5432/seminaire_platform?schema=public"

# Secrets HMAC distincts (séparation des clés) — générer avec `openssl rand -hex 32`.
FORM_SIGNING_SECRET="changeme"
CONSENTEMENT_HASH_SECRET="changeme"
